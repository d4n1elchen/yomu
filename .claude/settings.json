{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "permissions": {
    "allow": [
      "Bash(npm run:*)",
      "Bash(npm test:*)",
      "Bash(npm ci)",
      "Bash(npm ls:*)",
      "Bash(npx tsc:*)",
      "Bash(npx eslint:*)",
      "Bash(npx prettier:*)",
      "Bash(npx vitest:*)",
      "Bash(git status:*)",
      "Bash(git diff:*)",
      "Bash(git log:*)",
      "Bash(git show:*)",
      "Bash(git branch:*)"
    ],
    "deny": [
      "Read(./.env)",
      "Read(./.env.local)",
      "Read(./.env.development.local)",
      "Read(./.env.production.local)"
    ]
  },
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ -n \"$(git status --porcelain -- src 2>/dev/null)\" ]; then printf '%s' '{\"systemMessage\":\"Uncommitted source edits - check docs/PLAN.md before wrapping up (delete shipped sections, correct stale not-built claims).\",\"hookSpecificOutput\":{\"hookEventName\":\"Stop\",\"additionalContext\":\"There are uncommitted changes under src/. If this chunk shipped planned work or changed a decision, reconcile docs/PLAN.md - it is the one file no test checks. If the plan genuinely needs nothing, say so and move on.\"}}'; fi",
            "statusMessage": "Checking PLAN.md freshness"
          }
        ]
      }
    ]
  }
}
